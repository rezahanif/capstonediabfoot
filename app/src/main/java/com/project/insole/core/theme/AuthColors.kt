package com.project.insole.core.theme

import androidx.compose.ui.graphics.Color

object AuthColors {
    // ─── Shared Brand Colors ──────────────────────────────────────────────────
    val GradientTop    = Color(0xFF114797)
    val GradientBottom = Color(0xFF112B4E)
    val ButtonBg       = Color(0xFF114784)
    val TextLight      = Color(0xFFEBF1FF)

    // ─── Component Specific ───────────────────────────────────────────────────
    val CardBg         = Color(0xFFC9DBF2).copy(alpha = 0.05f)
    val InputBorder    = Color(0xFF2A5BAD)
    val InputBg        = Color(0xFF1E4585)
    val Hint           = Color(0xFF8AAAD8)
    val Divider        = Color(0xFF2A5BAD)
}
